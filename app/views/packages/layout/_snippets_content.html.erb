<div class="tab-content">
  <% @active_show = true %>
  <% @package.snippets.each do |snip| %>
      <% if @active_show %>
          <div class="tab-pane active" id="tab<%= snip.id %>">
            <p><%= render partial: 'snippets/show', :locals => { :snippet => snip} %></p>
          </div>
          <% @active_show = false %>
      <% else %>
          <div class="tab-pane" id="tab<%= snip.id %>">
            <p><%= render partial: 'snippets/show', :locals => { :snippet => snip} %></p>
          </div>
      <% end %>
  <% end %>
</div>